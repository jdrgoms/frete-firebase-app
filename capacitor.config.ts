import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frete-firebase-app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
